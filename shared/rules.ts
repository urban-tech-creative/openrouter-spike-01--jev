// Game rules that Jev is told about in words (worker/jev.ts) and that the
// simulation enforces (src/game/simulation.ts). One source, so they can't drift.

export const FREEZE_RADIUS = 100;
export const FREEZE_SECONDS = 3;
export const FREEZE_RECHARGE_SECONDS = 8;
export const HEALTH_PACK_HEAL = 30;
